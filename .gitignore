# Local Claude Code settings (copy settings.local.json.example to start)
.claude/settings.local.json

# Local environment / secrets — never commit
.env
.env.*
*.local

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Swift Package Manager build artifacts
.build/
.swiftpm/
Package.resolved

# The concrete LaunchAgent is generated by `make install` from launchd/*.plist
# into ~/Library/LaunchAgents — nothing generated lands in the repo.
