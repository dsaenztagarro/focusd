# <Mechanism name>

*How <the mechanism> actually works today.* Complementary to [ADR NNNN](decisions/NNNN-....md), which records *why* it was built this way; this records *how* it behaves so the mental model can be rebuilt without reading all the code.

## The mental model

<One paragraph: what this mechanism is, what it's responsible for, and the one idea that makes
the rest make sense.>

## How it works

```
<ASCII diagram of the flow / states / components — optional but usually worth it>
```

<Step-by-step or component-by-component. Name the key files as you go:
`path/to/file` — what it does.>

## Failure modes

- **<condition>** → <what happens, and where it's handled>.
- **<condition>** → <…>.

## See also

- [ADR NNNN](decisions/NNNN-....md) — the decision behind this mechanism
- <related guide / feature doc>
